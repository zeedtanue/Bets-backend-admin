'use strict';

/**
 * job-opportunity controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::job-opportunity.job-opportunity');
